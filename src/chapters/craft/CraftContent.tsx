export function CraftContent() {
  return (
    <div className="chapter-copy">
      <p className="eyebrow">04 &mdash; Craft</p>
      <h2>How they&rsquo;re built.</h2>
      <p>
        SwiftUI throughout. Offline-first, so the apps work on a train.
        Postgres row-level security where money is involved. 729 tests across
        the two, and a release runbook that gets executed, not read.
      </p>
    </div>
  );
}
